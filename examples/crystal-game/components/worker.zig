//! Tag component — the second leg of the `["Hunger","Worker"]` query in
//! crystal/hunger.cr (the labelle-engine#742 HungerController shape,
//! ported to the native family).
pub const Worker = struct {};
