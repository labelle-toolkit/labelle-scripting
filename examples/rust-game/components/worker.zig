//! Tag component — the second leg of the `["Hunger","Worker"]` query in
//! rust/hunger.rs (the labelle-engine#742 HungerController shape, ported
//! to the native family).
pub const Worker = struct {};
