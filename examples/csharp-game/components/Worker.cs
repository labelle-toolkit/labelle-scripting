// Tag component — the second leg of the `["Hunger","Worker"]` query in
// scripts/HungerSystem.cs (the labelle-engine#742 HungerController shape,
// ported to the CoreCLR-hosted family). Declared in C# (labelle-scripting#27):
// a zero-field `[LabelleComponent]` record, the C# twin of crystal's
// `Labelle.component "Worker"`.
[LabelleComponent]
record Worker;
